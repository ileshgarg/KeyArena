version: '3.8'

services:
  keyarena-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: keyarena-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    command: ["npm", "run", "start"]

